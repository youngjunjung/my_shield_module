if SHIELD_PEC_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "peto"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_PEC_LEFT || SHIELD_PEC_RIGHT

config ZMK_SPLIT
    default y

endif